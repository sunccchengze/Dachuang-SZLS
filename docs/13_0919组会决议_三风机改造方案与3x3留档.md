# 13 · 2026-09-19 组会决议 · 数字孪生「3×3 → 单列三风机」改造方案与 3×3 留档

> 记录口径：本文第一节只记录**用户转述给本轮 agent 的决议**，会议原始记录与其它同学的分工不在本文范围；
> 阶段性总结的完整版见 [`materials/孙承泽——9月工作阶段总结.docx`](../materials/孙承泽——9月工作阶段总结.docx)（同日撰写）。
> 第二节起是**改造工程方案**：用户 2026-09-19 拍板"本轮先留档 + 出方案，改造本体下一轮开始"，
> 几何按"3 台沿主风向（北→南）串列，间距沿用 632 m = 5.02D"。

## 一、会议决议（与本模块相关的部分）

| # | 决议 | 对本仓的影响 | 状态 |
|---|---|---|---|
| 1 | 阶段性工作总结 + 未来规划（改动较大） | 阶段总结已成文并入库：`materials/`；规划要点见 docx §6.3（10–12 月 v1 真实数据换接 / 偏航链收口 / 协议层；1–3 月 v2 MADDPG 回放；4–5 月 v3 微缩风洞闭环；6 月结题） | 已入库 |
| 2 | 孙承泽新增**写材料、答辩 PPT** 等任务 | 新建 `materials/` 作为对外材料统一入口（索引 + 成品），可复用 `viz/` 里 8.23 组会 PPT 生成脚本 `viz/generate_823_pptx.py` 与 `skills/` 里的演示技能 | 目录已建，材料待用户给题目/时间点 |
| 3 | 数字孪生从 **3×3 九机阵** 改为 **单列三风机** | `twin/` 需要一次布局级改造（本文 §二–§五） | 方案已出，下一轮动工 |
| 4 | **3×3 不删**，留档，视研究进度可能调回 | 附注 tag `twin-3x3-archive-20260919` + 改造采用"布局可切换"设计（3×3 代码不删除） | 已留档 |
| 5 | 两个仓库合一，wind_farm_viz 迁入后删除 | 见 [`docs/12`](12_两仓归一_wind_farm_viz迁入与分支清理.md) | 已完成 |

## 二、改造目标与几何

- **目标布局 `1x3`**：3 台 NREL 5MW 沿主风向（北→南，+z）串列，T01 最上游、T03 最下游；间距 **632 m（5.02D）**；
  列位取现 3×3 的中列 `x = −100`（保持场心与升压站/海岸/取景关系不变），行位沿用 `z = −1272 / −640 / −8`。
  —— 等价于**保留 3×3 的中列、去掉左右两列**，几何上是现布局的严格子集，海床/岸线/森林/升压站/相机基准不需要动。
- **物理意义**：串列是尾流导向（wake steering）最纯粹的工况：上游偏航 → 尾流横向偏折 → 下游增益；
  `viz/` 的两机串列研究（8 m/s、5D、最优 +25°、全场 +8.13%）可直接延伸为三机串列的基线叙事；
  对应申请书阶段五"3×3 微缩风洞"的**单列试验段**（用户口径）。
- **不做的事**：不改海洋/地形/天空/声场/草地任何视觉层；不改 GCH 内核公式（它本来就是逐机顺序求解，与布局无关）。

## 三、设计原则：布局可切换，不删 3×3

```ts
// twin/src/scene/layouts.ts（新）
export type LayoutId = '3x3' | '1x3'
export const LAYOUTS: Record<LayoutId, { rowsZ: number[]; colsX: number[]; servos: number[]; label: string }> = {
  '3x3': { rowsZ: [-1272, -640, -8], colsX: [-732, -100, 532], servos: [6, 7, 3, 5, 1], label: '3×3 九机阵（FLORIS 例题场）' },
  '1x3': { rowsZ: [-1272, -640, -8], colsX: [-100],            servos: [0, 1, 2],       label: '单列三机串列（5.02D）' },
}
export const ACTIVE_LAYOUT: LayoutId = resolveLayout()   // 默认 '1x3'；URL ?layout=3x3 / 环境变量 VITE_LAYOUT 可切回
```

- `FARM` 由 `ACTIVE_LAYOUT` 生成，其余代码一律从 `FARM.length / FARM[i].row / FARM[i].col` 派生，**禁止再出现字面量 9 / 3×3**（selftest 加一条 grep 守卫）。
- 机位/锚点/开场路径/舵机映射等"按机组编号写死"的表，改为**按布局键查表**（`byLayout('3x3', …)`），3×3 那份原样搬进表里，不删。
- FLORIS 真值：3×3 的 oracle 与钦定表保留不动，新增 1×3 的 oracle；selftest 的 V&V 断言按 `ACTIVE_LAYOUT` 选对应真值组。
- 界面角标：布局属【示意】级（几何布置），角标文案随布局变化；页脚声明不变。

这样 3×3 有三重保险：Git tag（§六）、代码内切换（`?layout=3x3`）、oracle/文档不删。

## 四、逐文件改造清单（按 2026-09-19 代码实测 grep，145/0 自检基线）

### 4.1 布局真值源（先做，其它全部跟着变）

| 文件 | 现状 | 改法 |
|---|---|---|
| `twin/src/scene/terrainUtil.ts` | `FARM` 内联 `rowsZ×colsX` 双循环生成 9 台；`SERVOS=[6,7,3,5,1]`；`ROW_COUNT=3`；`ANCHOR` 写死 `FARM[4]/[6]/[7]/[8]`（中排中、近排左、近排中、近排右） | `FARM` 改由 `layouts.ts` 生成；`SERVOS/ROW_COUNT` 来自布局；`ANCHOR` 改为语义选择器（`upstream()/middle()/downstream()`），1×3 下 power→T02、wake→T01 北侧来流空域、turbine/dot→T03（最近景）、cable→T03↔升压站中点 |
| `twin/src/data/farmSim.ts` | `N_UNITS = FARM.length`（已派生 ✓）；**`FARM_RATED_MW = 45` 写死**；`optimizeYaw` 按 `row` 逐排贪心（`for r<3`） | `FARM_RATED_MW = N_UNITS * 5`；贪心循环上界改 `ROW_COUNT`（1×3 下每排 1 台，退化为逐机贪心，逻辑不变） |
| `twin/src/state/simStore.ts` | `ZERO_YAW` 长度、`SERVO_UNIT=[...SERVOS]`、注释"9 机" | 全部派生自 `N_UNITS/SERVOS`；HUD 需求功率上限 45→`FARM_RATED_MW`（4–15 MW 量程，控制台滑杆刻度随之） |
| `twin/src/data/telemetry.ts` | `p.unitYaw.length === 9` 硬校验 | 改 `=== N_UNITS`；DTAP 协议文档同步写明"长度 = 场内机组数" |

### 4.2 三维场景

| 文件 | 现状 | 改法 |
|---|---|---|
| `TurbineField.tsx` / `HoloTurbine.tsx` / `frameBus.ts` | 按 `FARM` 迭代（✓），注释"9 机共享" | 只改注释；几何共享机制不变 |
| `CableNetwork.tsx` | 3 条集电回路 = 每"排"西→东串接 3 台后汇入升压站（`for row<3`） | 1×3 下每排 1 台 → 改为**1 条回路沿列南下串接 3 台**再入站（或 3 条各 1 台的放射式；建议前者，符合一次接线且线条更少）；回路数 = 布局给定 |
| `AirflowField.tsx` | `FCX/FCZ` 场心由 `FARM` 求均值（✓）；`x9/z9` 数组按 `FARM.length` 分配（✓） | 变量名去 "9"；场心随布局自动落到中列 |
| `WindVeil.tsx` / `florisData.ts` | 粒子取风：FLORIS 真场覆盖区 = **T07→T04 双机走廊**（3×3 编号），其余 Jensen 代理 | 1×3 下走廊变为 T01→T02→T03 整列：`floris3dData.mjs` 现有的双机 5 工况可直接覆盖 T01→T02 段；T02→T03 段暂用代理并挂【代理】角标，待 1×3 oracle 出来后换真场 |
| `introProfile.ts` | 34 s 开场路径 13 个航点写死 `FARM[2]/[4]/[6]`（T03→T05→T07 对角线穿场） | 1×3 改为**沿列纵深推进**：北侧来流空域 → 掠 T01 → 侧绕 T02 → 低机位停在 T03 前（终点构图与现在 T07 低机位一致）；3×3 路径整表保留在 `byLayout` 里 |
| `hotkeys.ts` | 9 个热键机位 1–9 一一对应 T01–T09；selftest 断言"恰 9 个" | 1×3：1/2/3 = 三机高处俯拍，4/5/6 = 三机平视，7/8/9 = 三机低处仰拍（沿用现有三种取景语法）；断言改"= 布局定义的机位数" |
| `CameraRig.tsx` | 书签/默认注视点引用 `FARM[6]` | 改语义选择器（最近景机组） |
| `Callouts.tsx` / `SparkleGround.tsx` / `NightPulse.tsx` | 读 `FARM`（✓） | 校验避让与地面星芒在 3 机下不空洞（可能需把星芒密度按机组数补偿） |
| `audio/audioModel.ts` / `audioEngine.ts` | "9 机全上 Panner 太贵"→ 声部裁决 | 3 机可**全部上 PannerNode**（HRTF 方位更准），预算反而更宽；注释与 `qa_audio*.mjs` 口径同步 |

### 4.3 HUD

| 文件 | 现状 | 改法 |
|---|---|---|
| `hud/Hud.tsx` | `机组状态矩阵 3×3`（标题/aria/CSS grid 3 列）；`Array.from({length: 9})` 生成 9 根舵机滑杆；雷达光点按 `FARM`（✓） | 矩阵改 `N_UNITS` 驱动，1×3 用**竖排 3 格**（与场景南北串列同构，上=上游）并把标题改"机组状态 · 串列 1×3"；滑杆 `N_UNITS`；KPI"装机 45 MW"→`FARM_RATED_MW`（15 MW）；一键寻优文案"9 机偏航角"→"逐机" |
| `styles/theme.css` | 矩阵 3 列网格 | 加 `.matrix[data-layout='1x3']` 变体 |

### 4.4 物理内核与真值（**最关键：不能拿 3×3 的数字给 1×3 用**）

| 项 | 现状 | 改法 |
|---|---|---|
| `core/physics/gch.ts` / `evalFarm.ts` | 逐机顺序 GCH 求解；注释里的 "3×3" 指**转子采样网格 3×3（±D/4）**，与场布局无关 | 公式零改动；注释澄清"转子网格 3×3 ≠ 场布局" |
| `docs/research/scripts/generate_floris_gch_oracle.py` | 第 4 节生成"九机阵列 3×3@632m 北来风"oracle | 新增第 5 节：**三机串列 `[(0,0),(0,632),(0,1264)]`**，工况 = 8 m/s × 上游/中游偏航扫描（−30…+30 步 5°）+ 统一偏航 + 逐机贪心最优；输出 `floris_gch_oracle_v1.json` 追加 `tandem3` 块 + `florisGchOracle.ts` 同步；`pip install floris==4.6.6` 复现 |
| `scripts/selftest.mts` V&V 段 | `FLORIS none 8108.08 kW ±5%`、`unified+30° 9060.03`、`独立寻优 24.04% ±3pt`、G3 三组 3×3 断言 | 按 `ACTIVE_LAYOUT` 选真值组：3×3 保留原断言；1×3 新增 none / unified / greedy 三组 + 逐机功率；G2 双机断言两布局共用 |
| 钦定数字（README / docs/03 / 界面角标） | +24.04% / 8095.15 kW / 97.97% 等均为 3×3 或双机口径 | 1×3 的**全场增益数字必须来自新 oracle**，在此之前界面只能显示【代理】；文档三处（meta / README / 角标）同步，不许混写 |

### 4.5 脚本、文档、门禁

| 文件 | 改法 |
|---|---|
| `scripts/qa2.mjs` `probe.mjs` `calibrateWake.mts` | `for i<9` / `new Array(9)` → `N_UNITS` |
| `scripts/selftest.mts` | 新增守卫：① `src/` 内不得出现字面量 `length: 9` / `Array(9)` / `=== 9`；② `LAYOUTS` 两套都能生成合法 `FARM`（间距 632、id 连续、贴地 ≤12 m）；③ 1×3 下 `FARM` 是 3×3 中列的严格子集（几何回归） |
| `twin/README.md` | "唯一真值源 `farmFrame(tHours, unitYaw[9], …)`" → `unitYaw[N]`；调试键加 `?layout=3x3`；结构树加 `layouts.ts`；渲染计数三档基线**重新实测**（3 机 draw calls 会明显下降，不许沿用 267/642,556） |
| `README.md` / `HANDOFF_NEXT.md` / `docs/03` | 布局口径改"当前 1×3（阶段五单列试验段），3×3 留档可切换" |
| `docs/research/round41_单列三机改造.md`（下一轮新建） | 过程记录：before/after 同机位对比、三档计数、oracle 数字、selftest 增量 |

### 4.6 工作量与顺序（建议 2 轮）

1. **R41-A 骨架**：`layouts.ts` + `terrainUtil/farmSim/simStore/telemetry` 派生化 + HUD 数量派生 + 脚本去 9 + selftest 守卫 → 此时 `?layout=3x3` 必须与留档 tag 像素级一致（abdiff = 0），证明"没删 3×3"；
2. **R41-B 内容**：1×3 机位/开场/锚点/电缆/矩阵竖排 + 1×3 oracle + V&V 断言 + 三档基线重测 + 文档三处同步。

## 五、验收门槛（沿用 HANDOFF_NEXT 九条 + 本项专属）

- `?layout=3x3` 与 `twin-3x3-archive-20260919` 同机位同参 A/B 帧差 = 0（`scripts/abdiff.py`）；
- 1×3：`npm run verify` 四闸门全绿；selftest 断言数**只增不减**；
- 1×3 全场功率数字全部可溯源到新 oracle（none / unified / greedy），界面【代理】/【模拟】角标与文档一致；
- 开场 34 s 运镜、13 机位、集电回路、矩阵、雷达、告警、时间轴在 3 机下逐项截图留证；
- 三档画质单帧计数重新实测并回填 README（不沿用 3×3 数字）。

## 六、3×3 留档（本轮已做）

- Tag：`twin-3x3-archive-20260919`（附注），指向本轮收尾提交；该提交 `twin/` = 3×3、`npm run selftest` **145/0**、lint 0/0、tsc 0、build ✓（2026-09-19 沙箱实测）。
- 取回方法见 [`docs/12` §六](12_两仓归一_wind_farm_viz迁入与分支清理.md#六数字孪生-33-状态留档组会决议改单列三风机但-33-不删)。
- 改造完成后，3×3 仍可用 `?layout=3x3` 在同一份代码里跑出来；tag 只是"哪怕代码被改坏也有底"的最后一道保险。
